/**
 * Function that divides `a` over `b`
 * \param a number to divide
 * \param b number that divies
 * \returns a / b
 * \exception if `b` is `0` an exception is thrown
*/
float divide(const float a, const float b) {
    return a / b;
}